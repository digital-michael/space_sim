// Package ui provides generic camera, selection, and performance-toggle state
// for Space Sim.
//
// It depends on the engine package but not on Raylib, which lets the
// application keep input and camera behavior separate from renderer-specific
// types and draw code.
package ui